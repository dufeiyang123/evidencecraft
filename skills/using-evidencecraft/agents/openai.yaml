interface:
  display_name: "Route Evidencecraft Work"
  short_description: "Route report state and execution handoffs"
  default_prompt: "Use $using-evidencecraft to inspect this evidence-analysis workspace, request a new Run's execution choice when needed, and route exactly one responsibility."
