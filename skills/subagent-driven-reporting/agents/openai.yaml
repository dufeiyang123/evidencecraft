interface:
  display_name: "Run Subagent-Driven Report"
  short_description: "Run report packages through fresh workers"
  default_prompt: "Use $subagent-driven-reporting after this Run selects Subagent-Driven execution, dispatch one fresh worker per Work Package, and verify every handoff."
